// ============================================
// CONFIGURATION
// ============================================
// Change this to your Replit backend URL when deployed!
// For local testing, leave it empty or use "http://localhost:3000"
const BACKEND_URL = window.location.hostname === 'localhost' ? '' : 'https://VIRGIGAMES-BACKEND.YOUR-USERNAME.repl.co';
